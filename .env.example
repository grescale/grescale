DATABASE_URL="postgresql://postgres:password@localhost:5432/grescale"
JWT_SECRET="replace-with-a-long-random-secret"
PUBLIC_APP_URL="https://your-domain.example"
NODE_ENV="production"
PORT="8080"

# Keep disabled in production unless you explicitly trust all custom script authors.
ALLOW_CUSTOM_SCRIPTS="false"
